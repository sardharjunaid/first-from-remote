[project]
name = "first-from-remote"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
